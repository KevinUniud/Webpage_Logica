{
  "name": "testlogica-web",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node tools/check-js.mjs",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test"
  }
}
